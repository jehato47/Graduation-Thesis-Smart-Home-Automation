import 'package:flutter/material.dart';

class ControlItem {
  IconData? icon;
  String title;
  Color? color;

  ControlItem({
    this.icon,
    required this.title,
    this.color,
  });
}
